# Optional. Matcher and policy always run without this.
# If unset or the call fails, ambiguous cases escalate with reason AI_UNAVAILABLE.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
LLM_TIMEOUT_SECONDS=8
